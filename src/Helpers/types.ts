export interface ExperienceType {
  role: string;
  tags?: string[];
  description: string;
  company: string;
  achievements: string[];
  period: string;
  tech_stack?: string[];
  currently_employed?: boolean;
}
